server.port=8080
spring.application.name=service-emisor

# ---- RabbitMQ ----
# host/user/pass por variable de entorno; default localhost/guest para correr desde el IDE.
# En Docker, el compose setea SPRING_RABBITMQ_HOST=rabbitmq.
spring.rabbitmq.host=${SPRING_RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${SPRING_RABBITMQ_PORT:5672}
spring.rabbitmq.username=${SPRING_RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${SPRING_RABBITMQ_PASSWORD:guest}

# ---- Eventos (topic exchange) ----
# Estos nombres deben coincidir con los del service-receptor.
app.events.exchange=eventos
app.events.routing-key=demo.mensaje.creado
